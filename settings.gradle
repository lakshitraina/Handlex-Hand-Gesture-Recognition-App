pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)  // Use FAIL_ON_PROJECT_REPOS
    repositories {
        google()
        mavenCentral()
        maven { url "https://jitpack.io" } // Only if you use JitPack
    }
}

rootProject.name = "HandLex"
include ':app'